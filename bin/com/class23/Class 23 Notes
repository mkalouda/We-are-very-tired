Overriding :
When same method with same return type and same signature(name,parameters) exists in
two classes which have inheritance relationship.

Overriding Static:
When we have same methods in the parent child classes with static modifier, we say that we are hiding a method
by re-declaring it instead of over-riding a method